apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  name: vcluster-demo
  namespace: argocd
spec:
  destination:
    namespace: demo
    server: https://kubernetes.default.svc
  project: demo
  source:
    chart: vcluster
    helm:
      releaseName: demo
      valuesObject:
        controlPlane:
          advanced:
            podDisruptionBudget:
              enabled: true
              minAvailable: 1
          backingStore:
            etcd:
              deploy:
                enabled: true
                statefulSet:
                  extraArgs:
                  - --client-cert-auth=false
                  highAvailability:
                    replicas: 3
          coredns:
            deployment:
              replicas: 2
            enabled: true
            overwriteConfig: |-
              .:1053 {
                errors
                health
                ready
                kubernetes cluster.local in-addr.arpa ip6.arpa {
                  pods insecure
                  fallthrough in-addr.arpa ip6.arpa
                  ttl 30
                }
                prometheus 0.0.0.0:9153
                forward . /etc/resolv.conf
                cache 30
                loop
                reload
                loadbalance
              }
          distro:
            k8s:
              enabled: true
              version: v1.34.3
          ingress:
            enabled: false
          proxy:
            extraSANs:
            - demo.integratn.tech
          service:
            annotations:
              external-dns.alpha.kubernetes.io/hostname: demo.integratn.tech
            enabled: true
            spec:
              ports:
              - name: https
                port: 443
                protocol: TCP
                targetPort: 8443
              type: LoadBalancer
          serviceMonitor:
            enabled: true
            labels:
              cluster_role: vcluster
              environment: production
              vcluster_name: demo
              vcluster_namespace: demo
          statefulSet:
            highAvailability:
              replicas: 3
            image:
              repository: loft-sh/vcluster-oss
            imagePullPolicy: Always
            persistence:
              addVolumeMounts:
              - mountPath: /etcd-certs
                name: etcd-certs
                readOnly: true
              addVolumes:
              - name: etcd-certs
                secret:
                  secretName: demo-etcd-certs
              volumeClaim:
                enabled: true
                size: 10Gi
            resources:
              limits:
                cpu: "2"
                memory: 2Gi
              requests:
                cpu: 500m
                memory: 2Gi
            scheduling:
              podManagementPolicy: Parallel
              priorityClassName: system-cluster-critical
        deploy:
          metallb:
            enabled: true
        exportKubeConfig:
          server: https://demo.integratn.tech:443
        integrations:
          certManager:
            enabled: true
            sync:
              fromHost:
                clusterIssuers:
                  enabled: true
                  selector:
                    labels:
                      integratn.tech/cluster-issuer: letsencrypt-prod
          externalSecrets:
            enabled: true
            sync:
              fromHost:
                clusterStores:
                  enabled: true
                  selector:
                    matchLabels:
                      integratn.tech/cluster-secret-store: onepassword-store
            webhook:
              enabled: true
          metricsServer:
            enabled: false
        logging:
          encoding: json
        networking:
          advanced:
            clusterDomain: cluster.local
          replicateServices:
            fromHost:
            - from: default/kubernetes
              to: default/kubernetes
        rbac:
          clusterRole:
            enabled: true
            extraRules:
            - apiGroups:
              - ""
              resourceNames:
              - eso-onepassword-token
              resources:
              - secrets
              verbs:
              - get
              - list
              - watch
        sync:
          fromHost:
            ingressClasses:
              enabled: true
            secrets:
              enabled: true
              mappings:
                byName:
                  external-secrets/eso-onepassword-token: external-secrets/eso-onepassword-token
            storageClasses:
              enabled: true
          toHost:
            ingresses:
              enabled: true
            networkPolicies:
              enabled: false
            persistentVolumes:
              enabled: true
            pods:
              enabled: true
        telemetry:
          enabled: false
    repoURL: https://charts.loft.sh
    targetRevision: 0.31.0
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
