apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vcluster-dev-test-kubeconfig
  namespace: vcluster-dev-test
  labels:
    app: vcluster
    instance: dev-test
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: onepassword-store
    kind: ClusterSecretStore
  target:
    name: vcluster-dev-test-kubeconfig-external
    creationPolicy: Owner
  data:
    - secretKey: kubeconfig
      remoteRef:
        key: vcluster-dev-test-kubeconfig
        property: notesPlain
