apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vcluster-dev-test-onepassword-token
  namespace: vcluster-dev-test
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword-store
    kind: ClusterSecretStore
  target:
    name: vcluster-dev-test-onepassword-token
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef:
        key: onepassword-access-token
        property: credential
    - secretKey: vault
      remoteRef:
        key: onepassword-access-token
        property: vault
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vcluster-dev-test-kubeconfig-sync
  namespace: vcluster-dev-test
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: vcluster-dev-test-kubeconfig-reader
  namespace: vcluster-dev-test
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["vc-dev-test", "vcluster-dev-test-onepassword-token"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: vcluster-dev-test-kubeconfig-sync
  namespace: vcluster-dev-test
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: vcluster-dev-test-kubeconfig-reader
subjects:
  - kind: ServiceAccount
    name: vcluster-dev-test-kubeconfig-sync
    namespace: vcluster-dev-test
---
apiVersion: batch/v1
kind: Job
metadata:
  name: vcluster-dev-test-kubeconfig-sync
  namespace: vcluster-dev-test
  labels:
    app: vcluster
    instance: dev-test
    component: kubeconfig-sync
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: vcluster
        instance: dev-test
    spec:
      serviceAccountName: vcluster-dev-test-kubeconfig-sync
      restartPolicy: OnFailure
      volumes:
        - name: sync-data
          emptyDir: {}
      initContainers:
        # Wait for vcluster kubeconfig secret to be created
        - name: wait-for-kubeconfig
          image: bitnami/kubectl:latest
          volumeMounts:
            - name: sync-data
              mountPath: /shared
          command:
            - /bin/bash
            - -c
            - |
              echo "Waiting for vcluster kubeconfig secret vc-dev-test..."
              until kubectl get secret vc-dev-test -n vcluster-dev-test 2>/dev/null; do
                echo "Secret not found, waiting..."
                sleep 10
              done
              echo "Writing kubeconfig to shared volume..."
              kubectl get secret vc-dev-test -n vcluster-dev-test -o jsonpath='{.data.config}' | base64 -d > /shared/kubeconfig
              echo "Secret found!"
      containers:
        - name: sync-to-onepassword
          image: alpine:3.19
          env:
            - name: OP_CONNECT_HOST
              value: "https://connect.integratn.tech"
            - name: OP_CONNECT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vcluster-dev-test-onepassword-token
                  key: token
            - name: OP_VAULT_ID
              valueFrom:
                secretKeyRef:
                  name: vcluster-dev-test-onepassword-token
                  key: vault
            - name: NAMESPACE
              value: "vcluster-dev-test"
            - name: VCLUSTER_NAME
              value: "dev-test"
            - name: HOSTNAME
              value: "dev-test.integratn.tech"
            - name: API_PORT
              value: "443"
            - name: SERVER_URL
              value: "https://dev-test.integratn.tech:443"
            - name: OP_ITEM_NAME
              value: "vcluster-dev-test-kubeconfig"
          volumeMounts:
            - name: sync-data
              mountPath: /shared
          command:
            - /bin/sh
            - -c
            - |
              set -e
              apk add --no-cache ca-certificates curl jq

              if [ -n "${SERVER_URL}" ]; then
                echo "Rewriting kubeconfig server to ${SERVER_URL}"
                awk -v new_server="${SERVER_URL}" '
                  !done && $1=="server:" {print "    server: " new_server; done=1; next}
                  {print}
                ' /shared/kubeconfig > /shared/kubeconfig.rewritten
                mv /shared/kubeconfig.rewritten /shared/kubeconfig
              fi

              KUBECONFIG_CONTENT=

              VAULT_NAME="homelab"
              OP_CONNECT_HOST_CLEAN="$(printf '%s' "${OP_CONNECT_HOST}" | tr -d '\r\n')"
              OP_CONNECT_TOKEN_CLEAN="$(printf '%s' "${OP_CONNECT_TOKEN}" | tr -d '\r\n')"
              API_BASE="${OP_CONNECT_HOST_CLEAN%/}/v1"
              AUTH_HEADER="Authorization: Bearer ${OP_CONNECT_TOKEN_CLEAN}"

              echo "Syncing kubeconfig to 1Password item via Connect API: ${OP_ITEM_NAME}"

              VAULT_ID="${OP_VAULT_ID:-}"
              VAULT_ID="$(printf '%s' "${VAULT_ID}" | tr -d '\r\n')"
              if [ -z "${VAULT_ID}" ]; then
                VAULT_ID=$(curl -fsS -H "${AUTH_HEADER}" "${API_BASE}/vaults" | jq -r --arg name "${VAULT_NAME}" '.[] | select(.name==$name) | .id' | head -n1)
              fi
              VAULT_ID="$(printf '%s' "${VAULT_ID}" | tr -d '\r\n')"
              if [ -z "${VAULT_ID}" ]; then
                echo "Vault not found: ${VAULT_NAME}"
                exit 1
              fi

              ITEM_ID=$(curl -fsS -H "${AUTH_HEADER}" "${API_BASE}/vaults/${VAULT_ID}/items" | jq -r --arg title "${OP_ITEM_NAME}" '.[] | select(.title==$title) | .id' | head -n1)

              if [ -n "${ITEM_ID}" ]; then
                echo "Item exists, replacing..."
                ITEM_PAYLOAD=$(jq -n --arg id "${ITEM_ID}" --arg title "${OP_ITEM_NAME}" --arg vault "${VAULT_ID}" --arg notes "${KUBECONFIG_CONTENT}" '{id:$id,title:$title,vault:{id:$vault},category:"API_CREDENTIAL",notesPlain:$notes,fields:[{label:"kubeconfig",type:"CONCEALED",value:$notes}]}')
                curl -fsS -X PUT -H "${AUTH_HEADER}" -H "Content-Type: application/json" "${API_BASE}/vaults/${VAULT_ID}/items/${ITEM_ID}" -d "${ITEM_PAYLOAD}" >/dev/null
              else
                echo "Item not found, creating..."
                ITEM_PAYLOAD=$(jq -n --arg title "${OP_ITEM_NAME}" --arg vault "${VAULT_ID}" --arg notes "${KUBECONFIG_CONTENT}" '{title:$title,vault:{id:$vault},category:"API_CREDENTIAL",notesPlain:$notes,fields:[{label:"kubeconfig",type:"CONCEALED",value:$notes}]}')
                curl -fsS -X POST -H "${AUTH_HEADER}" -H "Content-Type: application/json" "${API_BASE}/vaults/${VAULT_ID}/items" -d "${ITEM_PAYLOAD}" >/dev/null
              fi

              echo "Kubeconfig synced successfully to 1Password"
