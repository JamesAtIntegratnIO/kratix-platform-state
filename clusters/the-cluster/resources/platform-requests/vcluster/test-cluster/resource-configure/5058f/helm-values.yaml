apiVersion: v1
kind: ConfigMap
metadata:
  name: test-vcluster-values
  namespace: vcluster-test
data:
  values.yaml: |
    controlPlane:
      distro:
        k8s:
          enabled: true
          version: "1.34"
      statefulSet:
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
    
    sync:
      toHost:
        pods:
          enabled: true
